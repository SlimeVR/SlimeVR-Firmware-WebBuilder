// In the format of [owner, [repo, [branches]]]
import firmares from './firmwares.json';
import boards from './boards-infos.json';
export const AVAILABLE_FIRMWARE_REPOS = firmares;
export const AVAILABLE_BOARDS = boards;
